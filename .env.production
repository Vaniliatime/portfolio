# The PHP handler that ships in public/api. Only the production build reads
# this file, so `next dev` keeps the mail-client fallback and never posts to a
# path that does not exist locally.
NEXT_PUBLIC_CONTACT_ENDPOINT=/api/contact.php
