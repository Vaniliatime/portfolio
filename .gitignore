node_modules/
.next/
.next-build/
out/
next-env.d.ts
*.tsbuildinfo

.env
.env.local
.env*.local

# Certificate PDFs carry certificate and candidate numbers, so keep them out of
# a public repo. Their contents are transcribed into src/content/resume.ts.
/certs/

.DS_Store
Thumbs.db
.vscode/
.idea/
CV.md
readme.md

# Local tool permissions, not project content.
.claude/

# Mail credentials for the contact handler. The example file is the only one
# that belongs in the repository.
public/api/config.php
mail-config.php

# Original screenshots and source images, kept out of the build so they are
# not uploaded with the site.
_zrodla/

# Certificate scans and previews carry certificate and candidate numbers, the
# same reason /certs/ is excluded.
.certs-preview/
/*.pdf

# One-off scripts kept locally, not part of the site.
/import-amtracker.mjs
/make-sheet.mjs
/read-certs.mjs
